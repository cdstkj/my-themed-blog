container:
  image: node:latest

build_task:
  node_modules_cache:
    folder: node_modules
    fingerprint_script: cat yarn.lock
    populate_script: yarn install 
  build_script: yarn run build
  public_artifacts:
    path: "public/*"
